name: example
description: example
version: 1.0.0+1
publish_to: 'none'
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  bloc_form_plus: ^0.3.0

dev_dependencies:
  lints: ^6.0.0
